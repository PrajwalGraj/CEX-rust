[package]
name = "exchange"
version = "0.1.0"
edition = "2024"


[dependencies]
engine = { path = "../engine" }
balance = { path = "../balance" }
domain = { path = "../domain" }
tokio = { version = "1", features = ["full"] }