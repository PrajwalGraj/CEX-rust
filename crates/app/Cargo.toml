[package]
name = "app"
version = "0.1.0"
edition = "2024"

[dependencies]

domain = { path = "../domain" }
engine = { path = "../engine" }
exchange = { path = "../exchange" }
tokio = { version = "1", features = ["full"] }